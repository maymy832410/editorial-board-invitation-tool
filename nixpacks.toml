[phases.setup]
nixPkgs = ["python311"]

[phases.install]
dependsOn = ["setup"]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd v2 && uvicorn app:app --host 0.0.0.0 --port $PORT --workers 2"
