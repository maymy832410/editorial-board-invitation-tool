FROM python:3.11-slim

WORKDIR /app

# Copy all project files
COPY . /app/

# Install v2 dependencies
RUN pip install --no-cache-dir -r v2/requirements.txt

# Expose port
EXPOSE 8000

# Run FastAPI app
CMD ["sh", "-c", "cd /app/v2 && uvicorn app:app --host 0.0.0.0 --port $PORT --workers 2"]
